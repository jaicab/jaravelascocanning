---
layout: layout.html
title: Jara Velasco Canning
description: Make your website!
---

  {%- for post in collections.work | reverse -%}
    <section class="section">
      <div class="section__header">
        <h3>{{ post.data.title }}</h3>
        <p>{{ post.data.description }}
          <br/>{{ post.data.role }}
          <br/>{{ post.data.timeline }}
        </p>
      </div>
      <div class="collection">
        {{ post.content | safe }}
      </div>
    </section>
  {%- endfor -%}
  